// 7. Recorriendo arreglos con forEach:
// Dado un arreglo let numeros = [2, 4, 6, 8, 10], usa forEach para imprimir cada número en la consola.

// Se crea un arreglo que contiene 5 numeros
let numeros = [2, 4, 6, 8, 10];

// Con el arreglo, se llama a la funcion for each, asignando a cada numero la funcion para ser impreso.
numeros.forEach(function(numero) {
  console.log(numero);
});